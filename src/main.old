// hello
#include <SFML/Graphics.hpp>
#include <iostream>

#define RES 100
#define WIDTH 1300
#define HEIGHT 1000

sf::RenderWindow window(sf::VideoMode(WIDTH, HEIGHT), "Synth", sf::Style::None);

void handle(sf::Event event);
void draw_grid(void);

int main(void) {
    const auto onClose = [&window](const sf::Event::Closed&) {
        window.close();
    }

    const auto onKeyPressed = [&window](const sf::Event::KeyPressed& key) {
        if (key.scancode == sf::Keyboard::Scancode::A)
            // play sound
    }

    while (window.isOpen()) {
        window.handleEvents(onClose, onKeyPressed);
        window.clear();
        // draw_grid();
        window.display();
    }
    return 0;
}

void draw_grid(void) {
    sf::Vertex line[2];
    for (int x = MAIN_FRAME; x < WIDTH; x++) {
        for (int y = 1; y < HEIGHT; y++) {
            if (x % RES == 0) {
                ;
            }

            if (y % RES == 0) {
                ;
            }
        }
    }
}
